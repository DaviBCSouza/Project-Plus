model User {
  id    Int     @id @unique @default(autoincrement())
  email String  @unique
  name  String?
}

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
